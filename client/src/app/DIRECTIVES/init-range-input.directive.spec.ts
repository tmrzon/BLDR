import { InitRangeInputDirective } from './init-range-input.directive';

describe('InitRangeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InitRangeInputDirective();
    expect(directive).toBeTruthy();
  });
});
