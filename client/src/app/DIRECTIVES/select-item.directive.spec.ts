import { SelectItemDirective } from './select-item.directive';

describe('SelectItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectItemDirective();
    expect(directive).toBeTruthy();
  });
});
